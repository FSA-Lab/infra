{{- if and .Values.cert-manager.clusterIssuer.enabled .Values.cert-manager.clusterIssuer.email .Values.secrets.cloudflare.apiToken }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ .Values.cert-manager.clusterIssuer.name }}
spec:
  acme:
    server: {{ .Values.cert-manager.clusterIssuer.server | quote }}
    email: {{ .Values.cert-manager.clusterIssuer.email | quote }}
    privateKeySecretRef:
      name: {{ .Values.cert-manager.clusterIssuer.privateKeySecretName }}
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef:
              name: {{ .Values.cert-manager.clusterIssuer.cloudflare.secretName }}
              key: {{ .Values.cert-manager.clusterIssuer.cloudflare.apiTokenKey }}
{{- end }}
